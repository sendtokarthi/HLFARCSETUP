<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            margin: 0;
            height: 100vh;
            background-color: #e0f7e9;
        }
        .header {
            background-color: #2e7d32;
            color: #fff;
            padding: 10px 20px;
            text-align: center;
            font-size: 24px;
        }
        .main {
            display: flex;
            flex-grow: 1;
        }
        .sidebar {
            width: 220px; /* Reduced width */
            background-color: #388e3c;
            color: #fff;
            display: flex;
            flex-direction: column;
            padding: 20px;
        }
        .sidebar h2 {
            color: #fff;
            margin-bottom: 20px;
        }
        .sidebar a {
            color: #fff;
            text-decoration: none;
            padding: 10px 15px;
            margin: 5px 0;
            display: block;
            border-radius: 4px;
            transition: background-color 0.3s, transform 0.3s;
            font-size: 18px; /* Increased text size */
        }
        .sidebar a:hover {
            background-color: #1b5e20;
            transform: translateX(5px);
        }
        .sidebar a:active {
            background-color: #145a14;
        }
        .content {
            flex-grow: 1;
            padding: 20px;
        }
        .content h1 {
            margin-top: 0;
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
    </style>
    <script>
        function loadContent(url) {
            document.getElementById('contentFrame').src = url;
        }
    </script>
</head>
<body>
    <div class="header">
        HMS Archival Management
    </div>
    <div class="main">
        <div class="sidebar">
            <a href="javascript:void(0);" onclick="loadContent('/module-form')">Master Table Updation</a>
            <a href="javascript:void(0);" onclick="loadContent('/job-clean-up')">Job Clean Up</a>
            <a href="javascript:void(0);" onclick="loadContent('/reports')">Reports</a>
        </div>
        <div class="content">
            <iframe id="contentFrame" src=""></iframe>
        </div>
    </div>
</body>
</html>